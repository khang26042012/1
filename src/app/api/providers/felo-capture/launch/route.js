// Felo's launch button uses the generic capture launch route (same behavior —
// detect OS + installed Chromium browser and start it with the CDP port).
export { POST } from "../../capture/launch/route.js";
